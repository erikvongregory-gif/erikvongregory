import { SiteFooter } from "@/components/SiteFooter";

/** Desktop-Home: dunkler Kontakt-Footer (identisch mit Mobile via SiteFooter). */
export function DesktopFooter() {
  return <SiteFooter footerId="desktop-footer" />;
}
